services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ieee-office-backend:latest
    container_name: ieee-office-backend
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
    env_file:
      - .env